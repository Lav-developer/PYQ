<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              Site deep links (unverified https links — Android offers the app
              in a chooser; the website keeps working exactly as before).
              Tapping e.g. https://dsmnru-pyq.netlify.app/pyq/<slug> can open
              the PAPER SCREEN natively — MainActivity forwards the URL to the
              app's JS router. Verified Android App Links would require
              publishing /.well-known/assetlinks.json with the release signing
              fingerprint, deliberately deferred until a release keystore flow
              exists.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="dsmnru-pyq.netlify.app" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="dsmnru-pyq.email" />
            </intent-filter>

        </activity>

    </application>

    <!-- Permissions -->
    <!-- The dedicated app UI is bundled in the APK; its only data path is the
         existing Cloudflare Worker API (HTTPS), so INTERNET is the single
         required permission. DownloadManager needs no permission, and no
         storage permission is requested or needed.
         POST_NOTIFICATIONS / FCM are intentionally NOT added yet — the project
         stays ready for the planned notification feature (Google Services
         plugin auto-applies when a non-committed google-services.json exists,
         and MainActivity already forwards notification deep links through the
         same siteDeepLink channel used by share links). -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
